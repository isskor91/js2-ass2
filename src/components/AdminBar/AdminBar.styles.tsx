import styled from 'styled-components';
import { FlexBox } from '../FlexBox/FlexBox';

export const BaseSideBar = styled(FlexBox)`
  width: 100%;
  padding-left: 8px;
  box-shadow: 5px 1px 15px rgba(0, 0, 0, 0.2);
  flex-wrap: wrap;
`;
